@model MVCMASCOTAS.Models.ViewModels.ChangePasswordViewModel

@{
    ViewBag.Title = "Cambiar Contraseña";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Hero Section simple -->
<section class="py-4">
    <div class="container">
        <div class="text-center">
            <h1 class="display-5 fw-bold mb-2 text-brown">
                <i class="fa fa-key text-green"></i> Cambiar Contraseña
            </h1>
            <p class="lead mb-3 text-secondary">Actualiza tu contraseña para mantener tu cuenta segura.</p>
        </div>
    </div>
</section>

<!-- Main Content -->
<section class="py-5 bg-light-mint">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card border-green shadow-hover">
                    <div class="card-header bg-green text-white">
                        <h3 class="text-center mb-0">
                            <i class="fa fa-user-lock"></i> Cambiar Contraseña
                        </h3>
                    </div>
                    <div class="card-body p-4 p-md-5">
                        @using (Html.BeginForm("ChangePassword", "Account", FormMethod.Post, new { @class = "needs-validation", novalidate = "novalidate", id = "changePasswordForm" }))
                        {
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary(true, "", new { @class = "alert alert-green border-green" })

                            <div class="form-group mb-4">
                                <label for="CurrentPassword" class="form-label text-brown fw-medium">
                                    <i class="fa fa-lock text-green"></i> Contraseña Actual *
                                </label>
                                @Html.PasswordFor(m => m.CurrentPassword, new
                                {
                                    @class = "form-control border-green",
                                    placeholder = "Ingresa tu contraseña actual",
                                    required = "required",
                                    id = "CurrentPassword",
                                    autocomplete = "current-password"
                                })
                                @Html.ValidationMessageFor(m => m.CurrentPassword, "", new { @class = "text-danger small mt-1" })
                            </div>

                            <div class="form-group mb-4">
                                <label for="NewPassword" class="form-label text-brown fw-medium">
                                    <i class="fa fa-key text-green"></i> Nueva Contraseña *
                                </label>
                                @Html.PasswordFor(m => m.NewPassword, new
                                {
                                    @class = "form-control border-green",
                                    placeholder = "Mínimo 8 caracteres",
                                    required = "required",
                                    minlength = "8",
                                    id = "NewPassword",
                                    autocomplete = "new-password"
                                })
                                @Html.ValidationMessageFor(m => m.NewPassword, "", new { @class = "text-danger small mt-1" })
                                <small class="form-text text-secondary mt-2">
                                    <i class="fa fa-info-circle text-green"></i> La contraseña debe tener al menos 8 caracteres, incluir letras y números
                                </small>
                            </div>

                            <div class="form-group mb-4">
                                <label for="ConfirmPassword" class="form-label text-brown fw-medium">
                                    <i class="fa fa-key text-green"></i> Confirmar Nueva Contraseña *
                                </label>
                                @Html.PasswordFor(m => m.ConfirmPassword, new
                                {
                                    @class = "form-control border-green",
                                    placeholder = "Repite la nueva contraseña",
                                    required = "required",
                                    id = "ConfirmPassword",
                                    autocomplete = "new-password"
                                })
                                @Html.ValidationMessageFor(m => m.ConfirmPassword, "", new { @class = "text-danger small mt-1" })
                                <div id="passwordMatch" class="mt-2 d-none">
                                    <small class="text-success">
                                        <i class="fa fa-check-circle"></i> Las contraseñas coinciden
                                    </small>
                                </div>
                                <div id="passwordMismatch" class="mt-2 d-none">
                                    <small class="text-danger">
                                        <i class="fa fa-times-circle"></i> Las contraseñas no coinciden
                                    </small>
                                </div>
                            </div>

                            <div class="d-grid gap-3">
                                <button type="submit" class="btn btn-green btn-lg text-white">
                                    <i class="fa fa-save"></i> Cambiar Contraseña
                                </button>
                            </div>
                        }

                        <div class="text-center mt-4 pt-3 border-top border-green">
                            <a href="@Url.Action("MiPerfil", "Account")" class="btn btn-outline-green">
                                <i class="fa fa-arrow-left"></i> Volver a Mi Perfil
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Styles {
    <style>
        /* Estilos mínimos específicos - sin duplicar Layout */
        .form-control:focus {
            border-color: var(--green) !important;
            box-shadow: 0 0 0 0.2rem rgba(151, 199, 139, 0.25) !important;
        }

        .is-valid {
            border-color: var(--green) !important;
        }

        .is-invalid {
            border-color: #dc3545 !important;
        }
    </style>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

    <script>
        $(document).ready(function () {
            console.log('ChangePassword page loaded');

            // Validar que las contraseñas coincidan - VERSIÓN SIMPLE
            $('#ConfirmPassword').on('input', function () {
                var newPass = $('#NewPassword').val();
                var confirmPass = $(this).val();

                if (newPass === confirmPass && confirmPass !== '') {
                    $(this).removeClass('is-invalid').addClass('is-valid');
                    $('#passwordMatch').removeClass('d-none');
                    $('#passwordMismatch').addClass('d-none');
                } else if (confirmPass !== '') {
                    $(this).removeClass('is-valid').addClass('is-invalid');
                    $('#passwordMatch').addClass('d-none');
                    $('#passwordMismatch').removeClass('d-none');
                } else {
                    $(this).removeClass('is-valid is-invalid');
                    $('#passwordMatch').addClass('d-none');
                    $('#passwordMismatch').addClass('d-none');
                }
            });

            // Validación básica del formulario
            $('#changePasswordForm').submit(function (e) {
                var newPass = $('#NewPassword').val();
                var confirmPass = $('#ConfirmPassword').val();

                // Validar longitud mínima
                if (newPass.length < 8) {
                    e.preventDefault();
                    Swal.fire({
                        icon: 'error',
                        title: 'Contraseña muy corta',
                        text: 'La contraseña debe tener al menos 8 caracteres.',
                        confirmButtonColor: '#97C78B'
                    });
                    return false;
                }

                // Validar que coincidan
                if (newPass !== confirmPass) {
                    e.preventDefault();
                    Swal.fire({
                        icon: 'error',
                        title: 'Error',
                        text: 'Las contraseñas no coinciden. Por favor, verifica.',
                        confirmButtonColor: '#97C78B'
                    });
                    return false;
                }

                return true;
            });
        });
    </script>
}
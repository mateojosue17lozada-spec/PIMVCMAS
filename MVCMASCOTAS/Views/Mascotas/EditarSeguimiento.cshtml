@model MVCMASCOTAS.Models.SeguimientoAdopcion

@{
    ViewBag.Title = "Editar Seguimiento - ADOPTAMANIA";
    var mascota = ViewBag.Mascota as MVCMASCOTAS.Models.Mascotas;
    var contrato = ViewBag.Contrato as MVCMASCOTAS.Models.ContratoAdopcion;
}

<section class="seguimiento-hero py-4">
    <div class="container">
        <div class="d-flex flex-wrap justify-content-between align-items-center">
            <div>
                <h1 class="h2 text-brown mb-1">
                    <i class="fas fa-clipboard-check text-green me-2"></i>Editar Seguimiento #@Model.SeguimientoId
                </h1>
                <p class="text-secondary mb-0">
                    @Html.Encode(mascota?.Nombre) - @Html.Encode(mascota?.Especie)
                    @(!string.IsNullOrEmpty(mascota?.Raza) ? "(" + mascota.Raza + ")" : "")
                </p>
            </div>
            <div class="mt-2 mt-sm-0">
                <a href="@Url.Action("SeguimientoMascota", "Mascotas", new { id = mascota?.MascotaId })"
                   class="btn btn-outline-green">
                    <i class="fas fa-arrow-left me-2"></i>Volver a Seguimientos
                </a>
            </div>
        </div>
    </div>
</section>

<section class="py-4 bg-beige">
    <div class="container">

        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger rounded-pill mb-4">
                <i class="fas fa-exclamation-circle me-2"></i>@TempData["ErrorMessage"]
            </div>
        }

        <div class="row g-4 mb-4">
            <div class="col-md-6">
                <div class="info-card h-100">
                    <div class="info-card-header bg-green text-white">
                        <i class="fas fa-dog me-2"></i>Mascota
                    </div>
                    <div class="info-card-body">
                        @if (mascota != null)
                        {
                            <h5 class="text-brown mb-1">@Html.Encode(mascota.Nombre)</h5>
                            <p class="text-secondary mb-2">@Html.Encode(mascota.Especie) - @Html.Encode(mascota.Raza ?? "Mestizo")</p>
                        }
                        else
                        {
                            <p class="text-secondary mb-2">Datos de mascota no disponibles</p>
                        }
                        <hr class="border-green">
                        <p class="mb-1"><strong class="text-brown">Contrato:</strong> <span class="text-secondary">@Html.Encode(contrato?.NumeroContrato ?? "N/A")</span></p>
                        <p class="mb-0"><strong class="text-brown">Adoptante:</strong> <span class="text-secondary">@Html.Encode(contrato?.AdoptanteNombre ?? "No especificado")</span></p>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="info-card h-100">
                    <div class="info-card-header bg-green text-white">
                        <i class="fas fa-info-circle me-2"></i>Informacion del Seguimiento
                    </div>
                    <div class="info-card-body">
                        <p class="mb-1"><strong class="text-brown">ID Seguimiento:</strong> <span class="text-brown">#@Model.SeguimientoId</span></p>
                        <p class="mb-1"><strong class="text-brown">Fecha Original:</strong> <span class="text-secondary">@(Model.FechaSeguimiento?.ToString("dd/MM/yyyy HH:mm") ?? "No registrada")</span></p>
                        <p class="mb-0"><strong class="text-brown">Responsable:</strong> <span class="text-secondary">@Html.Encode(User.Identity.Name)</span></p>
                    </div>
                </div>
            </div>
        </div>

        <div class="form-card">
            <div class="form-card-header bg-green text-white">
                <i class="fas fa-edit me-2"></i>Editar Datos del Seguimiento
            </div>
            <div class="form-card-body">
                @using (Html.BeginForm("EditarSeguimiento", "Mascotas", FormMethod.Post, new { id = "formEditar" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(model => model.SeguimientoId)
                    @Html.HiddenFor(model => model.ContratoId)
                    @Html.HiddenFor(model => model.ResponsableSeguimiento)

                    @Html.ValidationSummary(true, "", new { @class = "alert alert-warning rounded-3 mb-3" })

                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label text-brown fw-medium">
                                <i class="fas fa-calendar-alt text-green me-2"></i>Fecha del Seguimiento *
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-white border-green"><i class="fas fa-calendar-alt text-green"></i></span>
                                @Html.TextBoxFor(model => model.FechaSeguimiento, "{0:yyyy-MM-dd}", new
                                {
                                    @class = "form-control border-green",
                                    type = "date",
                                    id = "FechaSeguimiento"
                                })
                            </div>
                            @Html.ValidationMessageFor(model => model.FechaSeguimiento, "", new { @class = "text-danger small mt-1" })
                        </div>

                        <div class="col-md-6">
                            <label class="form-label text-brown fw-medium">
                                <i class="fas fa-tag text-green me-2"></i>Tipo de Seguimiento *
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-white border-green"><i class="fas fa-tag text-green"></i></span>
                                @Html.DropDownListFor(model => model.TipoSeguimiento,
                                         new SelectList(ViewBag.TiposSeguimiento ?? new[] {
                                        "Inicial", "Primer Mes", "Tercer Mes", "Sexto Mes", "Anual",
                                        "Manual", "Extraordinario", "Queja", "Seguimiento Especial"
                         }, Model.TipoSeguimiento),
                                         "Seleccione tipo",
                                         new { @class = "form-select border-green" })
                            </div>
                            @Html.ValidationMessageFor(model => model.TipoSeguimiento, "", new { @class = "text-danger small mt-1" })
                        </div>

                        <div class="col-md-6">
                            <label class="form-label text-brown fw-medium">
                                <i class="fas fa-heart text-green me-2"></i>Estado de la Mascota *
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-white border-green"><i class="fas fa-heart text-green"></i></span>
                                @Html.DropDownListFor(model => model.EstadoMascota,
                                         new SelectList(ViewBag.EstadosMascota ?? new[] {
                                        "Excelente", "Bueno", "Regular", "Malo", "Enfermo", "Recuperandose", "Por evaluar"
                         }, Model.EstadoMascota),
                                         "Seleccione estado",
                                         new { @class = "form-select border-green" })
                            </div>
                            @Html.ValidationMessageFor(model => model.EstadoMascota, "", new { @class = "text-danger small mt-1" })
                        </div>

                        <div class="col-md-6">
                            <label class="form-label text-brown fw-medium">
                                <i class="fas fa-home text-green me-2"></i>Condiciones de Vivienda *
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-white border-green"><i class="fas fa-home text-green"></i></span>
                                @Html.DropDownListFor(model => model.CondicionesVivienda,
                                         new SelectList(ViewBag.CondicionesVivienda ?? new[] {
                                        "Excelentes", "Buenas", "Regulares", "Inadecuadas", "Temporales", "Por evaluar"
                         }, Model.CondicionesVivienda),
                                         "Seleccione condicion",
                                         new { @class = "form-select border-green" })
                            </div>
                            @Html.ValidationMessageFor(model => model.CondicionesVivienda, "", new { @class = "text-danger small mt-1" })
                        </div>

                        <div class="col-md-12">
                            <label class="form-label text-brown fw-medium">
                                <i class="fas fa-users text-green me-2"></i>Relacion con Adoptante *
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-white border-green"><i class="fas fa-users text-green"></i></span>
                                @Html.DropDownListFor(model => model.RelacionConAdoptante,
                                         new SelectList(ViewBag.Relaciones ?? new[] {
                                        "Excelente", "Buena", "Regular", "Mala", "Conflictiva", "Por evaluar"
                         }, Model.RelacionConAdoptante),
                                         "Seleccione relacion",
                                         new { @class = "form-select border-green" })
                            </div>
                            @Html.ValidationMessageFor(model => model.RelacionConAdoptante, "", new { @class = "text-danger small mt-1" })
                        </div>

                        <div class="col-md-12">
                            <label class="form-label text-brown fw-medium">
                                <i class="fas fa-images text-green me-2"></i>Ruta de Fotos de Evidencia
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-white border-green"><i class="fas fa-images text-green"></i></span>
                                @Html.TextBoxFor(model => model.FotosEvidencia, new
                                {
                                    @class = "form-control border-green",
                                    placeholder = "Ej: /evidencias/seguimiento/"
                                })
                            </div>
                        </div>

                        <div class="col-md-12">
                            <label class="form-label text-brown fw-medium">
                                <i class="fas fa-comment-alt text-green me-2"></i>Observaciones
                            </label>
                            @Html.TextAreaFor(model => model.Observaciones, new
                            {
                                @class = "form-control border-green",
                                rows = 5,
                                maxlength = "2000",
                                id = "Observaciones",
                                placeholder = "Describa el estado de la mascota, comportamiento, adaptacion..."
                            })
                            <div class="text-end small mt-1">
                                <span id="contadorObservaciones">@(Model.Observaciones?.Length ?? 0)</span>/2000
                            </div>
                        </div>

                        <div class="col-md-12">
                            <label class="form-label text-brown fw-medium">
                                <i class="fas fa-lightbulb text-green me-2"></i>Recomendaciones
                            </label>
                            @Html.TextAreaFor(model => model.Recomendaciones, new
                            {
                                @class = "form-control border-green",
                                rows = 5,
                                maxlength = "2000",
                                id = "Recomendaciones",
                                placeholder = "Recomendaciones especificas para el adoptante..."
                            })
                            <div class="text-end small mt-1">
                                <span id="contadorRecomendaciones">@(Model.Recomendaciones?.Length ?? 0)</span>/2000
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="form-check mt-2">
                                @Html.CheckBoxFor(model => model.RequiereIntervencion,
                                         new { @class = "form-check-input", id = "RequiereIntervencion" })
                                <label class="form-check-label text-brown fw-medium" for="RequiereIntervencion">
                                    <i class="fas fa-exclamation-triangle text-pink me-2"></i>Requiere intervencion del refugio
                                </label>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label text-brown fw-medium">
                                <i class="fas fa-calendar-plus text-green me-2"></i>Fecha Proximo Seguimiento
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-white border-green"><i class="fas fa-calendar-plus text-green"></i></span>
                                @Html.TextBoxFor(model => model.ProximoSeguimiento, "{0:yyyy-MM-dd}", new
                                {
                                    @class = "form-control border-green",
                                    type = "date",
                                    id = "ProximoSeguimiento"
                                })
                            </div>
                            <small class="text-secondary">Debe ser posterior a hoy</small>
                        </div>
                    </div>

                    <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mt-5 pt-4 border-top border-green">
                        <a href="@Url.Action("SeguimientoMascota", "Mascotas", new { id = mascota?.MascotaId })"
                           class="btn btn-outline-green">
                            <i class="fas fa-arrow-left me-2"></i>Cancelar
                        </a>
                        <button type="submit" class="btn btn-green btn-lg" id="btnGuardar">
                            <i class="fas fa-save me-2"></i>Actualizar Seguimiento
                        </button>
                    </div>
                }
            </div>
        </div>
    </div>
</section>

@section Styles {
    <style>
        .seguimiento-hero {
            background-color: var(--fondo-beige);
            border-radius: 0 0 30px 30px;
            margin-bottom: 1rem;
        }

        .info-card, .form-card {
            background: white;
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 15px 35px rgba(151,199,139,0.15);
            border: none;
        }

        .info-card-header, .form-card-header {
            padding: 1rem 1.5rem;
            font-weight: 600;
            font-size: 1.1rem;
        }

        .info-card-body, .form-card-body {
            padding: 1.5rem;
        }

        .input-group-text {
            border: 2px solid var(--verde-menta);
            border-radius: 16px 0 0 16px;
            background-color: white;
        }

        .form-control, .form-select {
            border: 2px solid var(--verde-menta);
            border-left: none;
            border-radius: 0 16px 16px 0;
            padding: 0.6rem 1rem;
        }

            .form-control:focus, .form-select:focus {
                border-color: var(--verde-menta);
                box-shadow: 0 0 0 0.2rem rgba(151,199,139,0.25);
            }

        .form-check-input:checked {
            background-color: var(--verde-menta);
            border-color: var(--verde-menta);
        }

        .btn-green {
            background-color: var(--verde-menta);
            border: none;
            border-radius: 50px;
            padding: 0.6rem 2rem;
            font-weight: 500;
            color: white;
            transition: all 0.3s;
        }

            .btn-green:hover {
                background-color: #86b57a;
                transform: translateY(-2px);
            }

            .btn-green:disabled {
                background-color: #a8c9a0;
                cursor: not-allowed;
                transform: none;
            }

        .btn-outline-green {
            border: 2px solid var(--verde-menta);
            background: transparent;
            color: var(--verde-menta);
            border-radius: 50px;
            padding: 0.6rem 2rem;
            font-weight: 500;
            transition: all 0.3s;
        }

            .btn-outline-green:hover {
                background-color: var(--verde-menta);
                color: white;
            }

        .border-green {
            border-color: var(--verde-menta) !important;
        }
    </style>
}

@section Scripts {
    <script>
        $(document).ready(function () {
            var hoy = new Date().toISOString().split('T')[0];
            $('#FechaSeguimiento').attr('max', hoy);

            var manana = new Date();
            manana.setDate(manana.getDate() + 1);
            $('#ProximoSeguimiento').attr('min', manana.toISOString().split('T')[0]);

            function actualizarContador(id, contadorId) {
                var len = $('#' + id).val().length;
                $('#' + contadorId).text(len);
                $('#' + contadorId).toggleClass('text-warning', len > 1800);
            }

            $('#Observaciones').on('input', function () { actualizarContador('Observaciones', 'contadorObservaciones'); });
            $('#Recomendaciones').on('input', function () { actualizarContador('Recomendaciones', 'contadorRecomendaciones'); });

            actualizarContador('Observaciones', 'contadorObservaciones');
            actualizarContador('Recomendaciones', 'contadorRecomendaciones');

            // Prevenir doble submit
            $('#formEditar').on('submit', function () {
                setTimeout(function () {
                    var btn = $('#btnGuardar');
                    btn.prop('disabled', true);
                    btn.html('<i class="fas fa-spinner fa-spin me-2"></i>Guardando...');
                }, 100);
            });
        });
    </script>
}
